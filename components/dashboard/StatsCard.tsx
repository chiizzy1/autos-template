import { FC } from "react";

interface StatsCardProps {}

const StatsCard: FC<StatsCardProps> = ({}) => {
  return (
    <div className=" bg-white shadow-lg border p-4 rounded-lg">StatsCard</div>
  );
};

export default StatsCard;